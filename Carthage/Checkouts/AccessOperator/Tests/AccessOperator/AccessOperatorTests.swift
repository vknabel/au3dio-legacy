import XCTest
@testable import AccessOperator

class AccessOperatorTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        // XCTAssertEqual(AccessOperator().text, "Hello, World!")
    }


    static var allTests : [(String, (AccessOperatorTests) -> () throws -> Void)] {
        return [
            ("testExample", testExample),
        ]
    }
}
